# Release Process

## Versioning

Use semantic versioning:

- `MAJOR` for breaking changes
- `MINOR` for backward-compatible features
- `PATCH` for fixes

## Release steps

1. update versions
2. update changelog
3. run tests
4. create tag
5. publish JavaScript and Python packages
6. announce release notes
